## meter todos los archivos nuevos
git add .
## meter descripción
git commit -m "final para pruebas"
git push
v
meter database.sql a mysql el nombre de la base de datos debe de ser restaurante1
ahi se pueden basar para los diagramas de la bd

en el .env deben de cambiar la contraseña y tambien en otro lugar ahi le buscan donde
donde ponga mysql-password cambienla por su contraseña de mysql

para iniciar el sistema es este comando:
 npm.cmd start

 y buscan en el navegador el localhost creo que era el 3001 y ahi se meten